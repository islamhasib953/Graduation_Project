import 'package:flutter/material.dart';

class MemoriesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vaccination'),
      ),
      body: Center(
        child: Text('Vaccination Screen Content'),
      ),
    );
  }
}
